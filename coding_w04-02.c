#include <stdio.h>  //ขาด # หน้าคำสั่ง include เป็น #include <stdio.h>
int main() {        //Keyword: int, Indentifier: main
    int a = 10;   //ไม่ได้ใส่ ; หลัง int a = 10

    printf("a = %d", a);  //ไม่ได้ใส่ ; หลัง printf("a = %d", a)

    return 0;  //ไม่ได้ใส่ ; หลัง return 0
}